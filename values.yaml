replicaCount: 1
image:
  repository: ghcr.io/fitnest-backend/frontend
  tag: "0b862abe82901abece2f2448dc1509e86d1a7542"
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - name: ghcr-secret
service:
  type: ClusterIP
  port: 80
  targetPort: 3000
istio:
  enabled: true
  gateway:
    name: public-gateway
  virtualService:
    hosts:
      - "fitnest.az"
    paths:
      - /
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
env:
  NODE_ENV: production
  NEXT_PUBLIC_API_URL: "https://dev-api.fitnest.az"
